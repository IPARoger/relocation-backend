.DS_Store
__pycache__/
venv/
*.pyc
*.log
Untitled
.env
ai_context/*.local.md
ai_context/private/
openai_api_key*
*.secret
.tmp-chrome-*/
.playwright-browsers/

# Phase 2.0 library scaffold persistence (local-only, never committed)
library/library.json
library/library.*.json

# Secrets / env files (never commit — keys live in GitHub Actions secrets)
.env.*
!.env.example
*api_key*
*API_KEY*
*.token
*.pdf

# GeoNames dumps (local only — download via import scripts)
data/geonames/alternateNamesV2.txt
data/geonames/alternateNamesV2.zip
data/geonames/*.txt
